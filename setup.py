from setuptools import setup
from monotonic_nn_surv_surf import __version__

setup(
    name='monotonic_nn_surv_surf',
    version=__version__,
    author='Yichen Chen',
    package_dir={
        "monotonic_nn_surv_surf": "monotonic_nn_surv_surf"
    }

)
